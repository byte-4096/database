#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <math.h>

//global constant values

#define max 5000

//void?
FILE *fs1;

//strings, so many of them...
char word = '!';
char *shift[];
char *str[max];
char *data[999];
char *ch[max];
char *type[max];
char *name;
char *fname[max];

//numbers
int counter = 0;
int done = 0;
int xss = 1;



//encodes using the one 
char encode(char data){

}

//decodes ceaser cipher, 
char cs1(char data){

}
